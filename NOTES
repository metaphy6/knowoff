1. The entire UI and its component should be re/configured different screen sizes in mind. Current web client screen is, for example, not fit in the view, let alone missing vertical scroll bar to see the rest
