{
  "$schema": "https://modelcontextprotocol.io/schema/mcp.json",
  "$comment": "VS Code Copilot MCP config. VS Code reads both this file and .mcp.json — codegraph is defined only in .mcp.json (edit its --path by hand for this machine/clone; this repo has no scaffold.sh) to avoid duplicate servers. Add other project-specific MCP servers here.",
  "servers": {},
  "inputs": []
}
